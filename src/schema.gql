# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Product {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime
  deletedAt: DateTime
  name: String!
  barcode: String
  unitType: UnitType!
  price: Float!
  currentStock: Float!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

"""Define os padrões de unidades dos produtos: kg (kilos) ou un (unidade)"""
enum UnitType {
  KG
  UN
}

type StockTransaction {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime
  deletedAt: DateTime
  type: TransactionType!
  quantity: Int!
  description: String
  productId: String!
}

"""Tipos de movimentação de estoque (Entrada, Saída, Perda)"""
enum TransactionType {
  IN
  OUT
  LOSS
}

type SaleItem {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime
  deletedAt: DateTime
  quantity: Float!
  unitPrice: Int!
  subTotal: Int!
  saleId: ID!
  productId: ID!
}

type Sale {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime
  deletedAt: DateTime
  totalAmount: Int!
  paymentMethod: PaymentMethod
  status: SaleStatus!
  saleItens: [SaleItem!]
}

"""Define os métodos de pagamento aceitos pelo sistema"""
enum PaymentMethod {
  PIX
  CREDIT_CARD
  MONEY
}

"""Status de vendas"""
enum SaleStatus {
  OPEN
  COMPLETED
  CANCELED
}

type PaginatedSalesDTO {
  items: [Sale!]!
  totalCount: Int!
}

type Query {
  findAll: [Product!]!
  findById(id: String!): Product!
  findByBarcode(barcode: String!): Product!
  stockExtractByProduct(id: String!): [StockTransaction!]!
  findAllSales(skip: Int = 0, take: Int = 20, startDate: DateTime, endDate: DateTime, status: SaleStatus, paymentMethod: PaymentMethod): PaginatedSalesDTO!
}

type Mutation {
  createProduct(data: CreateProductInput!): Product!
  updateProduct(id: String!, data: UpdateProductInput!): Product!
  removeProduct(id: String!): Product!
  createStockTransaction(data: CreateStockTransactionInput!): StockTransaction!
  createSale: Sale!
  addItemByBarcode(data: AddSaleItemByBarcodeInput!): SaleItem!
  completeSale(data: CompleteSaleInput!): Sale!
  cancelSale(id: String!): Sale!
}

input CreateProductInput {
  name: String!
  barcode: String
  unitType: UnitType!
  price: Int!
  currentStock: Int!
}

input UpdateProductInput {
  name: String
  barcode: String
  unitType: UnitType
  price: Int
}

input CreateStockTransactionInput {
  type: TransactionType!
  quantity: Int!
  description: String
  productId: String!
}

input AddSaleItemByBarcodeInput {
  saleId: ID!
  barcode: String!
}

input CompleteSaleInput {
  saleId: ID!
  paymentMethod: PaymentMethod!
}